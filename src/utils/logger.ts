import color, { level } from "chalk";

const error = color.redBright;
const warning = color.yellow;
const info = color.cyan;
const success = color.green;

export { color, level, error, warning, success, info };
